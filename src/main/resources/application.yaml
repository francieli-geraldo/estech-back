spring:
  main:
    banner-mode: off
  application:
    name: afine-se
  profiles:
    active: ${ENV_PROFILE:local}
  jackson:
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false
    deserialization:
      fail-on-unknown-properties: false
    mapper:
      accept-case-insensitive-enums: false
  mail:
    host: email-smtp.us-east-2.amazonaws.com
    port: 587
    username: AKIATBYRJVUTHNVPVZG2
    password: BCfH1lo829BH30dbO0/kimPIRvYd4BsopSxA5UN4Bclr
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

server:
  port: ${SERVICE_PORT}

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: info, health, metrics

feign:
  client:
    config:
      default:
        loggerLevel: basic

logging:
  level:
    com:
      jcraft:
        jsch: warn
